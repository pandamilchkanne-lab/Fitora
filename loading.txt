<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="manifest" href="manifest.json">
<meta name="theme-color" content="#0f0f0f">

<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>LiftLog – Loading</title>
<style>
    * {
        box-sizing: border-box;
    }

    body {
        margin: 0;
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        background: radial-gradient(circle at center, #111 0%, #050505 70%);
        overflow: hidden;
        font-family: Arial, sans-serif;
        color: white;
    }

    /* Logo Container */
    .logo-wrapper {
        position: relative;
        animation: fadeIn 0.6s ease forwards;
    }

    /* Main Logo */
    .logo {
        font-size: 64px;
        font-weight: bold;
        letter-spacing: 4px;
        opacity: 0;
        transform: scale(0.7) translateY(40px);
        animation: logoIn 2s cubic-bezier(.2,.8,.2,1) forwards;
    }

    /* Glow Line (Rockstar-Style Vibe) */
    .line {
        position: absolute;
        bottom: -15px;
        left: 50%;
        width: 0;
        height: 3px;
        background: linear-gradient(90deg, #ff8c3d, #1e90ff);
        transform: translateX(-50%);
        animation: lineIn 1.4s ease forwards;
        animation-delay: 1.4s;
    }

    /* Sub Text */
    .sub {
        margin-top: 35px;
        text-align: center;
        font-size: 14px;
        letter-spacing: 3px;
        opacity: 0;
        animation: fadeUp 1s ease forwards;
        animation-delay: 2.2s;
        color: #aaa;
    }

    /* Animations */
    @keyframes logoIn {
        0% {
            opacity: 0;
            transform: scale(0.7) translateY(40px);
        }
        100% {
            opacity: 1;
            transform: scale(1) translateY(0);
        }
    }

    @keyframes lineIn {
        from {
            width: 0;
        }
        to {
            width: 140px;
        }
    }

    @keyframes fadeUp {
        from {
            opacity: 0;
            transform: translateY(10px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @keyframes fadeIn {
        from { opacity: 0; }
        to { opacity: 1; }
    }
</style>
</head>
<body>

<div class="logo-wrapper">
    <div class="logo">LiftLog</div>
    <div class="line"></div>
    <div class="sub">HOLD ONE REP</div>
</div>

<script>
    // Nach der Animation automatisch zur Index-Seite wechseln
    setTimeout(() => {
        window.location.href = "index.html";
    }, 4200);
</script>

</body>
</html>
